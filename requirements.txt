# VERA full-project Python dependencies (preprocess + phases 1-5 + server tools).
# Recommended runtime: Linux, Python 3.10/3.11, NVIDIA driver + CUDA-compatible PyTorch.
# For a specific CUDA build, install torch/torchvision from pytorch.org first,
# then run: python -m pip install -r requirements.txt

# Core numerical, data, plotting, and preprocessing stack
numpy>=1.24,<2.0
pandas>=2.0,<3.0
Pillow>=10.0,<13.0
matplotlib>=3.7,<4.0
scikit-learn>=1.3,<2.0
tqdm>=4.66,<5.0
PyYAML>=6.0,<7.0
xgboost>=2.0,<4.0

# M1: frozen BioViL-T feature extraction
torch>=2.1
torchvision>=0.16
hi-ml-multimodal==0.2.2

# M2 detector and server-side Kaggle input bootstrap
ultralytics>=8.2,<8.5
kagglehub>=0.3,<2.0

# M2 scene-graph LLM branch.
# This code uses the TRL 0.11 SFTConfig/DataCollatorForCompletionOnlyLM API.
# Transformers 4.46+ is intentionally excluded because it breaks TRL <0.12.
transformers>=4.44,<4.46
trl==0.11.4
peft>=0.12,<0.14
bitsandbytes>=0.43,<0.46; platform_system == "Linux"
accelerate>=0.33,<1.0
datasets>=2.20,<3.0
